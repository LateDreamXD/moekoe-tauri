{
  "name": "moekoe-tauri",
  "version": "0.1.0",
  "description": "用 tauri 替换掉 moekoe music 的 electron",
  "scripts": {
    "postinstall": "unrun bin/postinstall.ts && pnpm run patch",
    "dev": "cd core && npm run dev",
    "build": "cd core && npm run build",
    "patch": "unrun bin/patch.ts",
    "postbuild": "pnpm api:build",
    "app:dev": "tauri dev",
    "app:build": "tauri build",
    "api:build": "unrun bin/api-build.ts"
  },
  "author": "晚梦 <xiaozhisans@qq.com>",
  "license": "GPL-2.0-only",
  "packageManager": "pnpm@10.28.0",
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.0",
    "@types/node": "^25.3.0",
    "unrun": "^0.2.27"
  },
  "dependencies": {
    "@tauri-apps/plugin-shell": "~2.3.5"
  }
}
